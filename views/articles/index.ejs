<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog Website</title>
    <style>
        body {
            background-color: #050505;
            border: 2px solid #f7f8fa;
            padding: 20px;
            box-sizing: border-box;
            margin: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .content {
            flex: 1;
        }
        footer {
            text-align: center;
            color: #fcfdfd;
            padding: 10px;
            border-top: 1px solid #f7f8fa;
        }
    </style>
</head>

<body>
    <div class="container content" style="text-align:center; max-width: 800px; margin: 0 auto; padding: 20px;">
        <h1 class="mt-4" style="text-align: center; color: #fcfdfd;">Abhishek Kumar Blog Website</h1>
        <a href="/articles/new" class="btn btn-success">Create Article</a>

        <% articles.forEach(article => { %>
            <div class="card mt-4" style="margin-bottom: 20px;">
                <div class="card-body">
                    <h4 class="card-title" style="font-size: 1.5em; color: #007bff;">
                        <%= article.title %>
                    </h4>
                    <div class="card-subtitle text-muted mb-2" style="font-size: 0.9em; color: #6c757d;">
                        <%= article.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2">
                        <%= article.description %>
                    </div>
                    <a href="articles/<%= article.slug %>" class="btn btn-primary" style="margin: 5px;">Read More</a>
                    <a href="articles/edit/<%= article.id %>" class="btn btn-info" style="margin: 5px;">Edit</a>
                    <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger" style="margin: 5px;">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    </div>
    <footer>
        &copy; 2024 Abhishek Kumar. All rights reserved.
    </footer>
</body>

</html>